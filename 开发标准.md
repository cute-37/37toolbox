# 37工具箱 开发标准

> **文档角色**: 本项目唯一的开发标准文件。所有 AI（Claude / Codex / 前端 AI）和未来的开发者都必须遵守。
> 本文档合并了原来的《开发规范》和《添加新工具指南》，并更新至 v0.2。
>
> **更新机制**: 当用户提出影响全局的意见时，Claude 更新对应章节并追加修订记录。Codex 和前端 AI 可提出规范建议，由 Claude 决定是否纳入。

---

## 第一部分：总则

### 1.1 项目信息

| 项 | 内容 |
|----|------|
| 项目名 | 37工具箱 |
| 类型 | Electron 桌面应用 |
| 技术栈 | Electron 31 · Vite 5 · React 18 · TypeScript 5 · Zustand 5 · Tailwind CSS 3 |
| 仓库 | `D:\Coding\projects\37工具箱` |
| 协作模式 | Claude(架构/Spec/CR) + Codex(后端/引擎/打包) + FrontendAI(UI/界面) |

### 1.2 目录结构

```
D:\Coding\projects\37工具箱/
├── 开发标准.md                    # 本文档（唯一权威标准）
├── package.json
├── tsconfig.json / tsconfig.node.json
├── vite.config.ts
├── electron-builder.yml
├── tailwind.config.ts             # 设计 Token
├── postcss.config.js
├── .gitignore
├── index.html
│
├── docs/                          # 📁 所有项目文档
│   ├── 总控计划.md                 # 架构 + Spec
│   ├── 给Codex的执行指令.md        # Codex 分派指南
│   ├── 给前端AI的执行指令.md       # 前端AI 分派指南
│   ├── 给Claude的执行指令.md       # Claude 分派指南
│   ├── 待办-v0.3.md               # 当前待办清单
│   ├── archive/                    # 历史文件
│   └── cr-reports/                 # CR 审查报告
│
├── 工具开发/                       # Pixiv 下载器开发目录
│   └── pixiv-downloader/          # Python bridge + 原项目文件
│
├── electron/
│   ├── main.ts                    # 主进程
│   ├── preload.ts                 # IPC 桥接
│   └── ipc/
│       ├── fileHandlers.ts
│       ├── clipboardHandlers.ts
│       ├── appHandlers.ts
│       └── dirHandlers.ts
│
├── src/
│   ├── main.tsx                   # React 入口
│   ├── App.tsx                    # 根组件
│   ├── vite-env.d.ts              # window.toolbox 类型声明
│   ├── styles/
│   │   └── globals.css            # CSS 变量 + Tailwind 指令 + base 重置
│   │
│   ├── core/                      # 内核（不放 UI）
│   │   ├── types.ts               # 全部 TS 类型定义
│   │   ├── PluginManager.ts       # 插件管理器
│   │   └── pluginRegistry.ts      # 内置工具注册
│   │
│   ├── stores/
│   │   └── appStore.ts            # Zustand 全局状态
│   │
│   ├── components/
│   │   ├── icons/
│   │   │   └── ToolIcon.tsx       # 图标映射组件
│   │   ├── layout/                # 布局骨架
│   │   │   ├── TopBar.tsx
│   │   │   ├── MenuBar.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   ├── SidebarItem.tsx
│   │   │   ├── CategoryGroup.tsx
│   │   │   ├── SearchBox.tsx
│   │   │   ├── ToolWorkspace.tsx
│   │   │   ├── Dashboard.tsx
│   │   │   ├── SettingsPanel.tsx
│   │   │   └── StatusBar.tsx
│   │   └── shared/                # 通用 UI 组件
│   │       ├── Button.tsx
│   │       ├── Input.tsx
│   │       ├── TextArea.tsx
│   │       ├── Select.tsx
│   │       ├── Switch.tsx
│   │       ├── Tooltip.tsx
│   │       ├── EmptyState.tsx
│   │       └── ContextMenu.tsx
│   │
│   ├── plugins/                   # 内置工具（每个工具一个目录）
│   │   ├── timestamp-convert/   → manifest.ts / engine.ts / Tool.tsx
│   │   ├── password-gen/        → 同上
│   │   ├── unit-convert/        → 同上
│   │   ├── calculator/          → 同上
│   │   ├── image-compress/      → 同上
│   │   ├── qrcode/              → 同上
│   │   ├── color-picker/        → 同上
│   │   ├── text-diff/           → 同上
│   │   ├── markdown-preview/    → 同上
│   │   ├── json-formatter/      → 同上
│   │   ├── base64/              → 同上
│   │   ├── regex-test/          → 同上
│   │   └── downloader/          → 同上
│   │
│   └── hooks/
│       ├── useActiveTool.ts
│       └── useKeyboard.ts
│
├── resources/                     # 静态资源
│   ├── icon.png                   # 托盘图标
│   ├── icon.ico                   # Windows 任务栏图标
│   └── fonts/
│       ├── Inter-Variable.woff2
│       └── JetBrainsMono-Variable.woff2
│
└── externals/                     # 外部插件示例
    └── example-plugin/
        ├── manifest.ts
        └── index.tsx
```

### 1.3 角色边界

| 角色 | 能做 | 不能做 |
|------|------|--------|
| **Claude** | 架构设计、Spec 编写、类型定义、代码审查、规范维护 | 写运行态代码（不输出 .tsx/.ts 实现文件，除了 types.ts 和 .css） |
| **Codex** | 脚手架、Electron 主进程、PluginManager、engine.ts 引擎函数、appStore、构建打包 | 写 UI/CSS、写 Tool.tsx |
| **前端 AI** | 所有 UI 组件（layout/shared/plugins/*/Tool.tsx）、CSS 样式、交互动画 | 写工具逻辑（必须从 engine.ts import）、写构建配置、写 Electron 代码 |

### 1.4 三层工具体系 ⚠️ 开发新工具前必须参考

所有工具按运行模式分三层。每次需要开发新工具时，Claude/Codex/前端 AI 必须**先判断属于哪一层，再决定技术方案**。错选层级可能导致后期重构代价巨大。

#### 第一层：容器内渲染（container）— 默认选择

- **定位**：轻量效率工具（计算器、转换器、解析器、查询器、文本处理、代码格式化等）
- **运行方式**：在 ToolWorkspace 内渲染 React 组件，共享主渲染进程
- **技术方案**：`manifest.ts + engine.ts + Tool.tsx`，标准插件架构
- **特征**：纯计算或轻量 UI，无独立进程，加载 <2MB，冷启动不感知
- **判断标准**：功能 ≤100KB 代码，无独立状态管理需求，无重型第三方库（>10MB）
- **估算占比**：未来约 80% 的工具应该在这一层

**判断话术**：「这个工具功能小、计算轻，直接用容器内模式就行——在 ToolWorkspace 里打开运行，不用单独开进程。」

#### 第二层：子进程 + 内置壳（child-process）

- **定位**：需要独立运行时但应内置管理的工具（下载器、爬虫、视频转码、AI 推理、自动化脚本）
- **运行方式**：前端在 ToolWorkspace 做控制面板展示进度，后端在独立子进程（Python/Node.js/ffmpeg）
- **技术方案**：参考 Pixiv 下载器模式 — `engine.ts` 通过 `window.toolbox.python.*` IPC 与子进程 JSON 行通信，`Tool.tsx` 展示实时日志和状态
- **特征**：子进程崩溃不影响主窗口，可主动 kill/restart，内存独立核算，kill 后主窗口回收全部前端内存
- **判断标准**：需要 CPU 密集计算、网络 I/O、长时间运行、独立包管理、或有重型依赖（≥50MB）

**判断话术**：「这个工具需要后台长时间跑 / 依赖 Python 环境 / 内存占用大，用子进程模式比较合适——前端只管控制面板和展示进度，实际工作跑在独立进程里，崩了也不影响工具箱。」

#### 第三层：外部工具注册（external）

- **定位**：真正的重型独立软件（Photoshop、Blender、Aseprite、SAI、OBS、DAW 等）
- **运行方式**：工具箱只管理入口（侧边栏图标 + 仪表盘卡片），点击通过 `shell:openExternal` 启动指定 .exe
- **技术方案**：manifest 新增 `external: true` + `execPath: string`，**不提供 Tool.tsx**
- **特征**：完全独立进程，工具箱不干预其运行状态，不占用 Electron 内存，不显示绿点和关闭按钮
- **判断标准**：已有独立 .exe 的桌面软件，不适合内嵌到 Electron 窗口

**判断话术**：「这个软件本身就是独立 .exe，不需要内嵌——我把它的入口加到仪表盘和侧边栏，点击直接启动，和工具箱互不干扰。」

#### 决策流程

```
用户提新工具需求
    ↓
它是独立 .exe 吗？ → 是 → 第三层 (external)，只配 manifest 入口
    ↓ 否
需要重型依赖 / 子进程 / 长时间运行？ → 是 → 第二层 (child-process)
    ↓ 否
    → 第一层 (container)，当前标准模式
```

#### 各层 manifest 字段差异

| 字段 | 第一层 | 第二层 | 第三层 |
|------|--------|--------|--------|
| `external` | 省略 | 省略 | `true` |
| `execPath` | 省略 | 省略 | `string`（exe 路径） |
| `engine.ts` | ✅ 纯函数 | ✅ IPC 桥接 | ❌ 不需要 |
| `Tool.tsx` | ✅ 标准 UI | ✅ 控制面板 | ❌ 不需要 |
| `hasSettings` | 可选 | `true`（路径/线程配置） | 可选 |
| `defaultSettings` | 可选 | ✅ 存储/路径/账号等 | 可选 |

---

## 第二部分：添加新工具

### 2.1 什么时候创建新工具

满足以下**全部**条件才创建新工具：

1. 有一个独立、可描述的功能（不是已有工具的扩展）
2. 功能的输入和输出是明确的
3. 功能不需要修改内核代码（types.ts / PluginManager.ts / appStore.ts）

如果是给已有工具新增能力（比如密码生成器加"生成多个密码"），修改那个工具的 engine.ts 和 Tool.tsx，**不要创建新工具**。

#### 2.1.1 先判断层级（新增）

在满足 2.1 条件后，**必须按 §1.4 三层工具体系判断该工具属于哪一层**，再参照对应层的技术方案执行。不要默认所有工具都用容器内渲染。

### 2.2 工具目录结构

```
src/plugins/{tool-id}/
├── manifest.ts    # 工具元信息（必须）
├── engine.ts      # 纯逻辑函数（必须）
└── Tool.tsx       # React UI 组件（必须）
```

- `{tool-id}` 必须用 **kebab-case**，全小写，不超过 30 字符。如 `password-gen`、`json-formatter`
- `{tool-id}` 不能与 `src/plugins/` 下已有目录重名
- 三个文件名固定不变

### 2.3 步骤 1：确定分类（v0.2 动态分类）

v0.2 起分类不再硬编码。用户的分类列表保存在 store 的 `categories: CategoryDef[]` 中，可增删改。

内置分类预设（`src/core/types.ts` → `BUILTIN_CATEGORIES`）：

| ID | 标签 | 说明 |
|------|------|------|
| `daily` | 日常效率 | 时间戳、密码、单位换算、计算器 |
| `image` | 图片处理 | 压缩、二维码、取色器 |
| `text` | 文本处理 | diff、markdown |
| `dev` | 编码开发 | JSON、Base64、正则 |
| `download` | 下载工具 | 下载器 |
| `custom` | 我的工具 | 外部插件、临时工具 |

新工具默认归入已有分类。如果确实需要新分类，必须：
- 至少 2 个工具能归入新分类
- 分类职责明确，不与已有分类重叠

**不需要修改 types.ts**。v0.2 起 `ToolCategory` 是 `string` 类型，任何字符串都是合法的分类 ID。用户可在设置面板中自行创建分类。

### 2.4 步骤 2：在 ToolIcon 注册图标

1. 打开 `src/components/icons/ToolIcon.tsx`
2. 在顶部的 `import { ..., NewIcon } from 'lucide-react'` 中追加新图标的 import
3. 在 `ICON_MAP` 对象中追加一行：`'icon-name': NewIcon,`

图标名必须是 lucide-react 中存在的图标名称（小写 kebab-case，如 `'qr-code'`、`'file-text'`）。

### 2.5 步骤 3：写 manifest.ts

```typescript
// @author: codex | phase: 4a | tool: {tool-id} | manifest
import type { ToolManifest } from '../../core/types';

export const manifest: ToolManifest = {
  id: '{tool-id}',              // kebab-case，唯一，确定后不要改
  name: '{工具名称}',            // 中文，不超过 8 字
  description: '{一句话描述}',    // 不超过 20 字
  category: 'daily',            // 从已有分类 ID 中选一个
  version: '1.0.0',
  icon: '{lucide-icon-name}',   // 必须在 ToolIcon.tsx 的 ICON_MAP 中存在
  tags: ['tag1', 'tag2'],       // 至少 2 个搜索关键词，小写英文
  hasSettings: true,            // 有设置面板则为 true
  defaultSettings: {},          // hasSettings 为 true 时需要
};
```

### 2.6 步骤 4：写 engine.ts

**职责**: 导出 manifest（同上）+ 所有纯函数。

**约束**:
- 函数为纯逻辑：不访问 DOM、不访问 localStorage、不调用 window API、不 import React
- 所有函数必须有完整的 TypeScript 类型标注（参数类型 + 返回类型）
- 每个导出函数必须有 JSDoc 注释
- 引擎函数之间可以互相调用

**模板**:

```typescript
// @author: codex | phase: 4a | tool: {tool-id} | engine
import type { ToolManifest } from '../../core/types';

// ====== Manifest ======

export const manifest: ToolManifest = {
  id: '{tool-id}',
  name: '{工具名称}',
  description: '{一句话描述}',
  category: 'daily',
  version: '1.0.0',
  icon: '{icon-name}',
  tags: ['tag1', 'tag2'],
  hasSettings: false,
};

// ====== 类型（如有自定义）======

export interface MyResult {
  // ...
}

// ====== 引擎函数 ======

/**
 * {函数功能简述}
 * @param input - {参数说明}
 * @returns {返回值说明}
 */
export function myFunction(input: string): { ok: true; output: string } | { ok: false; error: string } {
  // 纯逻辑实现...
}
```

### 2.7 步骤 5：写 Tool.tsx

**职责**: 接收 `ToolProps`，调用 engine 函数，渲染 UI。

**必须遵守**:

1. 默认导出 React 组件，类型为 `React.FC<ToolProps>`
2. 从 `./engine` import 引擎函数 — **禁止在 Tool.tsx 中重写工具逻辑**
3. 从 `../../components/shared` import 共享组件
4. 图标使用 `<ToolIcon name="xxx" />` — **禁止从 lucide-react 直接 import 图标**
5. 所有颜色使用 Tailwind class（如 `text-text-primary`、`bg-bg-secondary`）— **禁止硬编码色值**
6. 必须覆盖四种状态：
   - **空状态**: 无输入时给出引导提示
   - **正常态**: 有输入有输出
   - **错误态**: 红色提示 + 具体错误信息
   - **处理中**: 异步操作时显示 loading（如适用）
7. 文件不超过 300 行。超过则拆分子组件到 `./components/` 子目录
8. 文件顶部写标注：`// @author: frontend-ai | phase: 4b | tool: {tool-id} | ui`

**模板**:

```tsx
// @author: frontend-ai | phase: 4b | tool: {tool-id} | ui
import React, { useState, useCallback } from 'react';
import type { ToolProps } from '../../core/types';
import { manifest, myFunction } from './engine';
import { Button, Input, TextArea, EmptyState } from '../../components/shared';

const ToolName: React.FC<ToolProps> = ({ settings, onSettingsChange, onStatusChange, theme }) => {
  const [input, setInput] = useState('');
  const [output, setOutput] = useState<string | null>(null);
  const [error, setError] = useState<string | null>(null);

  const handleProcess = useCallback(() => {
    if (!input.trim()) return;
    setError(null);
    onStatusChange('running');
    const result = myFunction(input);
    if (result.ok) {
      setOutput(result.output);
      onStatusChange('success');
    } else {
      setError(result.error);
      onStatusChange('error', result.error);
    }
  }, [input, onStatusChange]);

  if (!input && !output) {
    return <EmptyState icon={manifest.icon} title={manifest.name} description={manifest.description} />;
  }

  return (
    <div className="flex flex-col gap-4">
      <Input value={input} onChange={setInput} placeholder="请输入..." />
      <Button variant="primary" onClick={handleProcess}>处理</Button>
      {error && <p className="text-xs text-status-error">{error}</p>}
      {output && <TextArea value={output} readOnly className="font-mono" />}
    </div>
  );
};

export default ToolName;
```

### 2.8 步骤 6：注册工具

打开 `src/core/pluginRegistry.ts`，在 `builtinPluginRegistry` 数组中添加 lazy loader：

```typescript
{ manifest: myToolManifest, loader: () => import('../plugins/{tool-id}/Tool'), builtin: true },
```

### 2.9 步骤 7：验证清单

- [ ] manifest.ts 所有字段完整，id 唯一且为 kebab-case
- [ ] engine.ts 函数签名完整（参数类型 + 返回类型 + JSDoc）
- [ ] engine.ts 函数为纯逻辑，无 DOM/React 依赖
- [ ] Tool.tsx 从 `./engine` import 引擎函数
- [ ] Tool.tsx 使用 Tailwind class 引用颜色
- [ ] Tool.tsx 覆盖四种状态
- [ ] Tool.tsx 不超过 300 行
- [ ] ToolIcon.tsx 中已注册图标
- [ ] pluginRegistry.ts 中已注册
- [ ] npm run dev 能切换、不报错
- [ ] 搜索能找到此工具
- [ ] 文件顶部有 @author 标注

---

## 第三部分：代码规范

### 3.1 命名规范

#### 文件命名

| 类型 | 规范 | 示例 |
|------|------|------|
| React 组件 | PascalCase | `SidebarItem.tsx` |
| 工具引擎 | 小写 | `engine.ts` |
| 工具清单 | 小写 | `manifest.ts` |
| 类型定义 | 小写 | `types.ts` |
| 工具目录 | kebab-case | `json-formatter/` |
| Hook | use 前缀 + PascalCase | `useActiveTool.ts` |
| IPC handler | 小写，{domain}Handlers | `fileHandlers.ts` |

#### 变量与函数

- **常量**: UPPER_SNAKE_CASE → `MAX_PLUGIN_COUNT`
- **函数**: camelCase，动词开头 → `formatJSON()`、`getActivePlugin()`
- **接口/类型**: PascalCase，名词 → `ToolManifest`
- **Props 类型**: 组件名 + Props → `ButtonProps`
- **事件处理**: handle + 事件 → `handleClick`
- **布尔变量**: is/has/can/should 前缀 → `isLoading`
- **Zustand action**: 动词开头 → `activateTool`
- **Zustand selector**: get 前缀 → `getFilteredPlugins`

### 3.2 TypeScript 规范

- **禁止 `any`** — 用具体类型或 `unknown`
- **禁止 `as` 断言** — 优先使用类型守卫
- **所有函数必须有返回类型标注**
- **Props 类型单独定义**，不混在组件中
- **类型与值分开导入**: `import type { X }` vs `import { Y }`

导入顺序（组间空一行）：
1. React 相关
2. 第三方库
3. 项目内核
4. 共享组件
5. 同目录文件

```typescript
import React, { useState } from 'react';

import type { ToolProps } from '../../core/types';

import { Button } from '../../components/shared';
import { manifest, myFunction } from './engine';
```

### 3.3 注释规范

**文件顶部必须标注**:
```typescript
// @author: codex | phase: 4a | tool: json-formatter | engine
// @author: frontend-ai | phase: 2 | component: Sidebar
// @author: claude | phase: 0 | spec: 总控计划
```

**导出函数必须有 JSDoc**:
```typescript
/**
 * 格式化 JSON 字符串
 * @param input - 原始 JSON 字符串
 * @param indent - 缩进空格数 2 或 4
 * @returns 格式化结果或错误信息
 */
```

**复杂逻辑加行内注释**: 说明为什么这么做，不是做了什么。

### 3.4 副作用隔离

- **engine.ts** — 纯函数，不访问 DOM/localStorage/window
- **Tool.tsx** — 可以调用 hooks、IPC、浏览器 API
- 副作用（文件读写、剪贴板、localStorage）只能在组件或 IPC 层处理

### 3.5 复杂度上限

- 单个 Tool.tsx ≤ 300 行（超出拆分子组件）
- 共享组件 ≤ 150 行
- engine.ts ≤ 400 行
- 单个函数 ≤ 40 行

---

## 第四部分：样式与设计系统

### 4.1 37 风格

项目名"37工具箱"的视觉灵感来自《重返未来：1999》角色 37。遵循以下原则：

- 只做**抽象引用**：象牙色底、青绿色点缀、哑金细线、古希腊几何秩序感、轻量数学/星象纹理
- **禁止**直接使用角色图片、头像、台词或可识别版权素材
- **默认主题为浅色** 37 风格。暗色作为备用主题
- 工具界面以可读性、信息密度和稳定性优先，**不是角色痛屏**

### 4.2 颜色系统（CSS 变量）

所有组件**禁止硬编码色值**。必须使用 Tailwind class 引用 CSS 变量。

| Token | 暗色值 | 用途 |
|-------|--------|------|
| `--bg-primary` | `#f6f0df` | 主背景 |
| `--bg-secondary` | `#ffffff` | 卡片、输入框背景 |
| `--bg-sidebar` | `#eeeeF2` | 侧边栏 |
| `--bg-chrome` | `#eeeeF2` | 菜单栏、chrome 区 |
| `--bg-hover` | `#e8e8ee` | 悬停态 |
| `--bg-active` | `#dedee8` | 选中态 |
| `--border` | `#d6d6df` | 分割线 |
| `--border-light` | `#c8c8d3` | 输入框边框 |
| `--text-primary` | `#202027` | 主文字 |
| `--text-secondary` | `#5b5b67` | 次要文字 |
| `--text-muted` | `#858592` | 禁用文字 |
| `--accent` | `#e8a850` | 暖金主强调色 |
| `--accent-hover` | `#d4953a` | 强调色悬停 |
| `--accent-subtle` | `rgba(232,168,80,0.12)` | 强调色背景 |
| `--accent-cyan` | `#2f9ca8` | 青绿色点缀 |
| `--accent-cyan-subtle` | `rgba(47,156,168,0.12)` | 青绿背景 |
| `--accent-ivory` | `#67542f` | 象牙色 |
| `--success` | `#4dab77` | 成功 |
| `--error` | `#e05555` | 错误 |
| `--warning` | `#e0a550` | 警告 |
| `--info` | `#5c9ecf` | 信息 |
| `--diff-added-bg` | `rgba(77,171,119,0.12)` | Diff 新增行 |
| `--diff-removed-bg` | `rgba(224,85,85,0.12)` | Diff 删除行 |
| `--geometry-line` | `rgba(180,128,54,0.18)` | 几何装饰线 |
| `--geometry-blue-line` | `rgba(47,156,168,0.14)` | 青蓝几何线 |

### 4.3 Tailwind class 速查

```tsx
// 背景
className="bg-bg-primary"          // 主背景
className="bg-bg-secondary"        // 卡片
className="bg-bg-sidebar"          // 侧边栏
className="bg-bg-chrome"           // 菜单栏
className="bg-bg-hover"            // hover

// 文字
className="text-text-primary"      // 主文字
className="text-text-secondary"    // 次要
className="text-text-muted"        // 禁用

// 强调
className="text-accent"            // 暖金色文字
className="bg-accent"              // 暖金色背景按钮
className="bg-accent-subtle"       // 暖金微妙背景（badge）

// 状态
className="text-status-success"    // 绿色文字
className="text-status-error"      // 红色文字

// 边框
className="border-border"          // 标准边框
className="border-border-light"    // 输入框边框

// 字体
className="font-ui"                // Inter 界面字体
className="font-mono"              // JetBrains Mono 等宽字体

// 圆角
className="rounded-sm"             // 4px（按钮）
className="rounded-md"             // 8px（卡片）
className="rounded-lg"             // 12px（弹窗）

// 阴影
className="shadow-sm"              // 微妙
className="shadow-md"              // 中等
className="shadow-lg"              // 强

// 尺寸
className="text-2xs"               // 11px（badge）
className="text-xs"                // 13px（描述）
className="text-sm"                // 14px（正文）
className="text-lg"                // 16px（标题）
className="text-xl"                // 20px（应用名）
```

### 4.4 共享组件 API

**Button** — `variant: 'primary' | 'secondary' | 'ghost'`, `size: 'sm' | 'md'`
**Input** — 通用输入框，支持 `prefix`/`suffix` icon
**TextArea** — 多行文本，支持 `showLineNumbers`
**Select** — 下拉选择，`options: { value: string; label: string }[]`
**Switch** — 开关，`checked: boolean`, `onChange`, `ariaLabel`
**Tooltip** — 提示气泡，`content: string`, `side: 'top' | 'bottom'`
**EmptyState** — 空状态占位，`icon`, `title: string`, `description?: string`
**ContextMenu** — 右键菜单，`items: ContextMenuItem[]`, `position: {x,y}`, `onClose`

**ToolIcon** — 图标渲染，`name: string`, `size?: number`。name 必须在 `ToolIcon.tsx` 的 `ICON_MAP` 中存在。

### 4.5 状态处理标准

每个 Tool.tsx 必须覆盖：

```
空状态 → 无输入时 → <EmptyState title="..." description="..." />
正常态 → 有输入有输出 → 正常 UI
错误态 → 处理失败 → 红色边框/文字 + 具体错误信息
处理中 → 异步进行中 → onStatusChange('running')，UI 显示 loading 状态
```

错误信息必须**具体可操作**：
```
✅ { ok: false, error: '第 3 行第 15 列 JSON 缺少逗号' }
❌ { ok: false, error: 'Error' }
```

---

## 第五部分：架构规范

### 5.1 Electron 安全

- `nodeIntegration`: **必须 false**
- `contextIsolation`: **必须 true**
- 渲染进程不直接使用 Node API → 全部走 preload IPC
- 禁止 `eval()` 和 `new Function()`
- 主窗口使用自绘标题栏 + 自定义窗口控制按钮，菜单栏为 React 自定义组件

### 5.2 IPC Channel 清单

| Channel | 方向 | 用途 |
|---------|------|------|
| `file:read` | 渲染→主 | 读取文本文件 |
| `file:write` | 渲染→主 | 写入文件 |
| `file:openDialog` | 渲染→主 | 打开文件选择 |
| `file:saveDialog` | 渲染→主 | 保存文件选择 |
| `clipboard:write` | 渲染→主 | 写入剪贴板 |
| `clipboard:read` | 渲染→主 | 读取剪贴板 |
| `app:getVersion` | 渲染→主 | 获取版本 |
| `app:quit` | 渲染→主 | 退出 |
| `app:toggleDevTools` | 渲染→主 | 开发者工具 |
| `shell:openExternal` | 渲染→主 | 外部浏览器 |
| `dir:scan` | 渲染→主 | 枚举目录 |
| `window:minimize` | 渲染→主 | 最小化 |
| `window:toggleMaximize` | 渲染→主 | 最大化/还原 |
| `window:close` | 渲染→主 | 关闭 |

### 5.3 状态管理边界

| 数据 | 存储位置 | 持久化 |
|------|---------|--------|
| 工具自身 UI 状态 | `useState`（Tool.tsx 内部） | 不持久化 |
| 插件列表 | `appStore.plugins` | 每次启动扫描 |
| 当前激活工具 | `appStore.activeToolId` | 不持久化 |
| 工具设置 | `localStorage` → `37toolbox:{id}` | 自动持久化 |
| 隐藏工具集合 | `localStorage` → `37toolbox:hidden-tools` | 自动持久化 |
| 自定义分类 | `localStorage` → `37toolbox:categories` | 自动持久化 |
| 主题 | `appStore.theme` + `document.documentElement` class | 不持久化 |

### 5.4 动态分类系统（v0.2）

- 分类由 `appStore.categories: CategoryDef[]` 管理
- 内置分类 `builtin: true` — 不可删除、不可改 id
- 用户可增删改自定义分类
- 删除分类时，该分类下工具自动移到 `custom`
- `ToolManifest.category` 类型为 `string`（任何分类 ID 均合法）
- `CATEGORY_LABELS` 和 `CATEGORY_ORDER` 已废弃，使用 `getCategoryLabel(categories, id)` 和 `sortCategories(categories)` 替代

---

## 第六部分：Git 与打包

### 6.1 提交规范

- 每完成一个 Phase 提交一次
- 提交信息用中文：`[Phase N] 简短描述`
- `.gitignore` 排除：`node_modules/` `dist/` `dist-electron/` `release/` `*.log` `.DS_Store` `Thumbs.db`

### 6.2 构建验证

- `npm run dev` — 启动 Electron 窗口，确认功能正常
- `npm run build` — 必须零报错、零 TS 警告
- `npm run package` — 产出 Windows .exe 安装包

### 6.3 图标资源

| 文件 | 格式 | 用途 |
|------|------|------|
| `resources/icon.png` | PNG 256×256 | 托盘图标 + macOS 图标 |
| `resources/icon.ico` | ICO（16/32/48/256） | Windows 任务栏 + 安装包图标 |
| `src/assets/logo-37-app.png` | PNG | 应用内品牌 Logo |

---

## 第七部分：完整工具示例

以下是一个完整的工具示例——字数统计（`src/plugins/word-count/`）：

### manifest.ts
```typescript
// @author: codex | phase: 4a | tool: word-count | manifest
import type { ToolManifest } from '../../core/types';
export const manifest: ToolManifest = {
  id: 'word-count', name: '字数统计', description: '统计文本字符、行数',
  category: 'text', version: '1.0.0', icon: 'type',
  tags: ['word', 'count', 'character'], hasSettings: false,
};
```

### engine.ts
```typescript
// @author: codex | phase: 4a | tool: word-count | engine
import type { ToolManifest } from '../../core/types';
export const manifest: ToolManifest = { /* 同上 */ };

export interface WordCountStats { characters: number; words: number; lines: number; bytes: number; }

/** 统计文本各项指标 */
export function countWords(text: string): WordCountStats {
  return {
    characters: text.length,
    words: text.trim() ? text.trim().split(/\s+/).length : 0,
    lines: text ? text.split('\n').length : 0,
    bytes: new TextEncoder().encode(text).length,
  };
}
```

### Tool.tsx
```tsx
// @author: frontend-ai | phase: 4b | tool: word-count | ui
import React, { useState, useEffect } from 'react';
import type { ToolProps } from '../../core/types';
import { manifest, countWords, type WordCountStats } from './engine';
import { TextArea, EmptyState } from '../../components/shared';

const WordCountTool: React.FC<ToolProps> = ({ onStatusChange }) => {
  const [text, setText] = useState('');
  const [stats, setStats] = useState<WordCountStats | null>(null);

  useEffect(() => {
    if (!text) { setStats(null); onStatusChange('idle'); return; }
    setStats(countWords(text));
    onStatusChange('success');
  }, [text, onStatusChange]);

  if (!text) return <EmptyState icon="type" title="字数统计" description="输入文本查看统计" />;

  return (
    <div className="flex flex-col gap-4">
      <TextArea value={text} onChange={setText} placeholder="输入或粘贴文本..." className="font-mono min-h-48" />
      {stats && (
        <div className="grid grid-cols-4 gap-3">
          {[['总字符', stats.characters], ['单词', stats.words], ['行数', stats.lines], ['字节', stats.bytes]]
            .map(([label, value]) => (
              <div key={label} className="bg-bg-secondary border border-border rounded-md p-3 text-center">
                <div className="text-lg font-semibold text-text-primary">{value.toLocaleString()}</div>
                <div className="text-xs text-text-secondary mt-1">{label}</div>
              </div>
            ))}
        </div>
      )}
    </div>
  );
};

export default WordCountTool;
```

---

## 第八部分：修订记录

| 日期 | 版本 | 修订人 | 内容 |
|------|------|--------|------|
| 2026-06-26 | v2.0 | Claude | 合并《开发规范》+《添加新工具指南》为统一标准，更新至 v0.2（动态分类、ToolIcon、ContextMenu、Dashboard、SettingsPanel、设计 Token 完整清单） |
| 2026-06-27 | v2.1 | Claude | 新增 §1.4 三层工具体系（容器内/子进程/外部工具），开发新工具前必须判定层级。同步更新项目记忆和 Claude 执行指令 |

---

> **文档末尾**: 本文档是 37 工具箱项目的唯一开发标准。所有 AI 和开发者在开始任何工作前必须先阅读本文档。有规范建议时通过 Claude 更新。
