# 37工具箱 v0.4 待办清单

> **上一份**: `docs/待办-v0.3.md`（v0.3 对 Codex 和前端 AI 基本完成）

---

## 一、工具市场 — 实现（Codex + 前端 AI）

### Claude 已完成
- `docs/工具市场-完整设计.md` — 完整规范
- `src/core/types.ts` — PacketManifest / InstalledPackage / MarketplaceAPI / VALID_PERMISSIONS 类型
- `src/core/packetValidator.ts` — validatePacket / validateFileList / generatePermissionWrapper
- PluginManagerAPI 已声明市场接口

### Codex (Phase M1-M3)
1. PluginManager 实现 installFromPath / installFromUrl / uninstall / getInstalledPackages
2. ZIP 解压（用 Node.js `adm-zip`）+ 校验 + 复制到 `~/37工具箱/plugins/{id}/`
3. 安装记录持久化 (localStorage key: `37toolbox:installed`)
4. 权限注入：安装时根据 permissions 字段用 generatePermissionWrapper 包裹入口

### 前端 AI (Phase M4-M5)
1. SettingsPanel 新增"工具市场"Tab
2. 浏览/搜索/安装按钮 + 权限确认弹窗 + 进度条
3. 卸载：SidebarItem 右键"卸载"选项

---

## 二、自动更新（Codex，需要 GitHub Repo）

### 前置
用户创建 GitHub 仓库 + Personal Access Token

### 步骤
1. `npm install electron-updater --save-dev`
2. `electron-builder.yml` 加 `publish.provider: github`
3. `main.ts` 加 `autoUpdater.checkForUpdatesAndNotify()`
4. 手动上传 exe 到 GitHub Releases

---

## 三、Codex 执行清单（全量）

按顺序做，做完一件打 `[完成]`：

- [x] A: `npm run build` 验证编译（Claude 新增了 errorHandlers / errorCapture / packetValidator / types 扩展）
- [x] B: 实测 Pixiv 下载器（Python bridge 连接、账号添加、设置保存、同步/下载）
- [x] C: 验证外部工具注册代码逻辑
- [x] D: 验证设置版本迁移逻辑
- [x] E: 修复小问题（追番季度默认、errorCapture import 顺序）
- [x] F: M1: PluginManager 实现 installFromPath + uninstall + getInstalledPackages
- [x] G: M2: ZIP 解压引擎（安全校验 + 文件复制）
- [x] H: M3: 安装记录持久化 + 权限注入桥接
- [x] I: 清理临时文件
- [x] J: `npm run package`

---

## 四、完成后报告

在本文档末尾追加：

```
## Codex 执行记录 (2026-06-28)

### 已完成
- 列出每项完成情况

### 阻塞项
- 自动更新: 等用户创建 GitHub 仓库

### 给 Claude / 前端 AI 的备注
- 你发现的问题写在这里
```

## 前端 AI 执行记录 (2026-06-28)

### 已完成
- SettingsPanel 新增「工具市场」入口，并接入已安装工具卡片、手动安装、进度提示和 toast。
- 新增 PermissionDialog，安装前展示 .37tool manifest 中声明的权限。
- SidebarItem 对非内置工具追加右键「卸载工具」操作。
- Tooltip 已支持 rich HTML 内容，默认仍按纯文本渲染。

### 给 Claude 的备注
- 工具市场 UI 已通过 pluginManager 的 installFromPath / uninstall / getInstalledPackages 调用 Electron market IPC。
- 安装记录额外写入 name / icon / author / permissions，便于前端展示；核心 InstalledPackage 类型仍保持兼容。

## Codex 执行记录 (2026-06-28)

### 已完成
- [完成] A: `npm run build` 通过，TypeScript 与 Vite 构建零报错；仅保留 Vite chunk size 警告。
- [完成] B: Pixiv 下载器本日补测通过：Python bridge 就绪、统计栏可读、添加账号能出授权 URL、设置保存能同步到 Python settings.json、刷新可更新统计；真实 Pixiv 授权/下载仍需用户账号。
- [完成] C: 外部工具注册代码审查通过：Sidebar/Dashboard 对 `external=true` 调用 `window.toolbox?.shell?.openExternal(entry.manifest.execPath)`，不走普通 activate；外部工具不显示关闭小圆点。
- [完成] D: 设置版本迁移逻辑正确：`getSettings()` 比较 `_v` 与 `manifest.settingsVersion ?? 1`，旧版本用 defaults 补齐并写回。
- [完成] E: `src/main.tsx` 第一行已最先导入 `./errorCapture`；追番季度默认逻辑此前已修为选择最新可用季度。
- [完成] F: `PluginManager` 已实现 `installFromPath` / `installFromUrl` / `uninstall` / `getInstalledPackages`。
- [完成] G: 新增 `electron/ipc/marketHandlers.ts`，使用 `adm-zip` 完成 .37tool 校验、ZIP 安全解压、越界路径拒绝、失败回滚、URL 下载。
- [完成] H: 安装记录持久化到 `37toolbox:installed`，并附带 name/icon/author/permissions；安装时生成权限注入 bootstrap。
- [完成] I: 清理检查完成：`yuc_202607.html` 与 `fetch_yuc.py` 不存在；未删除 Pixiv 下载器自身 Python 源码。
- [完成] J: `npm run package` 通过，产物 `release/37工具箱 Setup 0.1.0.exe`，大小 104,298,102 bytes。

### 阻塞项
- 自动更新仍需要用户创建 GitHub 仓库，并把 `electron-builder.yml` 中 `owner: REPLACE_WITH_GITHUB_USERNAME` 换成真实账号后再发布 Release。
- Pixiv 真实同步/下载需要有效 Pixiv 账号 Refresh Token；当前只验证到 bridge、设置同步和授权 URL 流程。

### 给 Claude / 前端 AI 的备注
- 工具市场后端已接通 preload `window.toolbox.market`，UI 可调用 inspect/install/download/uninstall。
- `.37tool` 安装后统一写入 `index.js` 引导文件，因此包内入口不必强制叫 index；原入口由 bootstrap 动态导入。
- 权限桥修正了 file 权限判断：`file_read` 或 `file_write` 任一为 true 时才开放 `window.toolbox.file`。
